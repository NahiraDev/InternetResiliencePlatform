# Dependency Injection Guide

The kernel DI container supports singleton, scoped, transient, factory-created, named, lazy services with lifecycle hooks. Services are resolved through scopes and never imported directly by subsystem implementations.
