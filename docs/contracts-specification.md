# Contracts Specification

Kernel contracts are versioned by namespace and expose named operations. Each operation declares its required capability and is executed with a `KernelContext`. Initial namespaces are DNS, VPN, proxy, tunnel, routing, storage, security, telemetry, metrics, plugin, AI, notifications, configuration, health, and scheduler.
