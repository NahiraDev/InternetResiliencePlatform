# SDK Guide

Future plugins depend on `@platform/sdk`. The SDK re-exports public kernel contracts, context, capability, workflow, and runtime helpers while avoiding internal implementation dependencies.
