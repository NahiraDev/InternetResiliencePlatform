# DNS Cache Management

The in-memory DNS cache records entries, hit count, misses, hit ratio, expiration, and flush counts. Cache management supports explicit flushing, expiration sweeps, warming domains from configuration, and integration points for platform DNS cache flushing.
