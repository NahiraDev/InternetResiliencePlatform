# Phase 21 Remediation Plan

| Order | Finding | Priority  | Issue                                                                                                                                    | Why it matters                                                              | Dependency                          | Complexity | Verification method                                                                      |
| ----- | ------- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ----------------------------------- | ---------- | ---------------------------------------------------------------------------------------- |
| 1     | P21-001 | P1 High   | Repository format gate fails on four pre-existing files.                                                                                 | CI/quality gate cannot be green.                                            | None                                | Small      | `pnpm exec prettier --check .` plus targeted regression tests                            |
| 2     | P21-002 | P1 High   | Coverage command fails because network-intelligence function coverage is 88.28% (<90) and branch coverage is 76.34% (<80).               | Coverage gate is broken even though unit tests pass.                        | None                                | Medium     | `pnpm coverage` plus targeted regression tests                                           |
| 3     | P21-003 | P1 High   | Desktop depends on demo fixtures and reports backend control API unavailable; runtime launch was not verified in this audit environment. | Desktop is not a verified live client for backend/control subsystems.       | Architecture decision / prior fixes | Medium     | `Inspect demo-data and run desktop dev/package commands.` plus targeted regression tests |
| 4     | P21-004 | P1 High   | DNS/routing/tunnel/AI packages build and test independently but are not wired into backend API or desktop as live services.              | Major subsystem completion cannot be claimed beyond library-level behavior. | Architecture decision / prior fixes | Medium     | `rg imports from apps/api apps/desktop packages/*` plus targeted regression tests        |
| 5     | P21-005 | P2 Medium | Several packages use vitest --passWithNoTests and report success with no test files.                                                     | Test status overstates actual coverage.                                     | Architecture decision / prior fixes | Medium     | `pnpm test output` plus targeted regression tests                                        |
| 6     | P21-006 | P2 Medium | API falls back to a development JWT secret outside tests when JWT_SECRET is unset.                                                       | Unsafe default can reach non-production runtime if env is misconfigured.    | Architecture decision / prior fixes | Medium     | `rg JWT_SECRET apps/api/src/index.ts` plus targeted regression tests                     |
| 7     | P21-007 | P2 Medium | Contains expect(true).toBe(true), a superficial assertion.                                                                               | Test suite includes a non-behavioral assertion.                             | Architecture decision / prior fixes | Small      | `rg "expect\(true\)" packages/core/src` plus targeted regression tests                   |
| 8     | P21-008 | P2 Medium | Schema/migration exist but no real database migration/runtime connection was verified; tests use client abstraction.                     | Persistence readiness is not proven.                                        | Architecture decision / prior fixes | Medium     | `pnpm test; inspect database package` plus targeted regression tests                     |
| 9     | P21-009 | P3 Low    | Prior reports mark phases complete where Phase 21 found demo-only or unverified integration.                                             | Documentation overstates readiness.                                         | Architecture decision / prior fixes | Small      | `Compare docs to runtime/integration evidence.` plus targeted regression tests           |

## Gate Decision

NO-GO. Do not start Phase 22 until P1 blockers are resolved and runtime integration is verified.
