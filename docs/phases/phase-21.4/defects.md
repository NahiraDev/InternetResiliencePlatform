# Phase 21.4 Defects

| ID       | Severity | Subsystem             | Reproduction                                                                                                                                                                             | Root cause                                                                                                  | Fix                                                                                                                                       | Regression test                                                                                                                   | Verification                                                                                                                                        |
| -------- | -------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| P214-001 | P1       | Test architecture     | Run `pnpm --filter @irp/events test`, `@irp/queue`, `@irp/shared`, `@irp/auth`, `@irp/telemetry`, or `@irp/utils` before this phase and observe `--passWithNoTests`/no behavioral tests. | Runtime/infrastructure packages had test scripts that could pass without tests.                             | Added behavioral tests and removed `--passWithNoTests` from those packages.                                                               | New tests in `packages/events`, `packages/queue`, `packages/shared`, `packages/auth`, `packages/telemetry`, and `packages/utils`. | Targeted package tests pass under Node v22.22.2.                                                                                                    |
| P214-002 | P2       | Test quality          | `rg "expect\(true\)" packages/core/src`.                                                                                                                                                 | Core scheduler test ended with a non-behavioral assertion.                                                  | Replaced with a cancellation assertion proving the scheduled job did not run after cancellation/shutdown.                                 | `packages/core/src/index.test.ts` scheduler cancellation test.                                                                    | Package-isolated run is blocked by unresolved workspace package entry for `@irp/logger`; root Turbo test remains required after build graph repair. |
| P214-003 | P1       | CI coverage           | Inspect `.github/workflows/ci.yml`; no fresh forced coverage gate existed.                                                                                                               | CI could pass `pnpm test` without proving coverage execution independent of Turbo cache.                    | Added `pnpm exec turbo run test --force -- --coverage` to CI.                                                                             | CI workflow coverage gate.                                                                                                        | Not executed locally end-to-end in this turn due remaining workspace graph/test blockers.                                                           |
| P214-004 | P0       | Package manager audit | NVM Node 22 install invoked its configured default global package installation.                                                                                                          | `/root/.nvm/default-packages` caused an implicit global package installation while switching Node versions. | No repository files were changed by that global install; final package-manager result is NO-GO because `npmCommandsExecuted` is not zero. | Result report records the violation.                                                                                              | Environment/package-manager gate is FAIL for this run.                                                                                              |
