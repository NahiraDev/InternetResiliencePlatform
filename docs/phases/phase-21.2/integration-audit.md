# Phase 21.2 Integration Audit

| Subsystem          | Implementation                                                                | Interface                                           | Registration                                               | Caller                                        | Runtime path                                                         | Status       | Evidence                                                                                                                                    |
| ------------------ | ----------------------------------------------------------------------------- | --------------------------------------------------- | ---------------------------------------------------------- | --------------------------------------------- | -------------------------------------------------------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------- |
| Backend API        | Fastify server in `apps/api/src/index.ts`                                     | HTTP `/api/v1/*` routes                             | `buildServer()` registers routes and hooks                 | runtime `apps/api/src/index.ts` startup       | client -> route -> service objects -> response                       | COMPLETE     | `/api/v1/health`, `/api/v1/ready`, `/api/v1/health/network`, `/api/v1/probes/run` were queried against a real process.                      |
| Network health     | `NetworkMonitoringService` in `@irp/network`                                  | `runOnce()`, `snapshot()`, `measurements()`         | API constructs one monitor and records telemetry           | health/probe routes                           | route -> monitor -> probes -> telemetry metrics -> JSON              | COMPLETE     | Phase 21.2 changed health endpoint to execute one real probe pass when no measurements exist.                                               |
| Connectivity       | probes and `ConnectivityMonitor` in `@irp/network`                            | probe classes and monitor status API                | default probes in `NetworkMonitoringService`               | backend network routes, CLI                   | backend route -> default probes -> local interfaces/DNS/TCP/HTTP     | PARTIAL      | Safe runtime generated real interface/throughput/provider measurements, but external DNS/TCP/HTTP were blocked/timed out in this container. |
| Routing            | `@irp/routing` package                                                        | package exports                                     | package-level only                                         | no live backend/Electron runtime caller found | none verified at runtime                                             | DISCONNECTED | No host route mutation was executed. Route application remains outside the live API/UI path.                                                |
| DNS                | `@irp/dns` package and DNS probe in `@irp/network`                            | resolver/transport interfaces and DNS latency probe | package-level; network probe registered by default monitor | backend probe route                           | backend -> network DNS latency probe -> Node DNS lookup              | PARTIAL      | Ordinary DNS lookup path is live, but secure DNS package is not used by backend/desktop runtime.                                            |
| Secure DNS         | DoH/DoT transport code exists in `@irp/dns`                                   | secure transport types/selection APIs               | package tests only                                         | no live backend/Electron caller found         | no verified runtime path                                             | DISCONNECTED | Repository contains DoH/DoT implementation code and tests, but no backend/core registration consumes it in live runtime.                    |
| Recovery           | retry loops in network monitor and package-level failover/recovery code       | retry/failover APIs                                 | monitor retries configured; other packages package-level   | backend probes route for monitor retry only   | probe failure -> retry -> measurement/failure count                  | PARTIAL      | Retry behavior exists for probes; cross-subsystem recovery orchestration is not wired as a live path.                                       |
| Tunnel             | `@irp/tunnel` package and desktop demo state                                  | package APIs/demo data                              | package-level/demo only                                    | no real provider started                      | none verified at runtime                                             | PARTIAL      | No real tunnel provider is configured or started; UI tunnel data is DEMO.                                                                   |
| Security           | backend auth/RBAC/helmet, Electron security settings, `@irp/security` package | HTTP auth, Electron preload IPC, policy APIs        | backend hooks/routes; Electron BrowserWindow/preload       | API clients and renderer                      | backend requests -> auth/RBAC; renderer -> preload allowlist -> IPC  | PARTIAL      | Static Electron security and backend auth tests pass; Electron runtime starts only with root `--no-sandbox` in container.                   |
| AI decision engine | deterministic package/demo decision data                                      | decision APIs/demo IPC                              | package/demo only                                          | desktop IPC returns demo decision             | renderer -> preload -> IPC -> demo-data                              | PARTIAL      | Existing deterministic behavior is not wired to live backend network context.                                                               |
| Event bus          | `InMemoryEventBus`                                                            | publish/subscribe API                               | API creates an in-memory bus                               | auth registration route                       | route -> publish in process                                          | PARTIAL      | In-process events work; no cross-process backend-to-Electron bus is present.                                                                |
| Observability      | `@irp/telemetry` metrics and logger                                           | Prometheus render + structured logs                 | API network telemetry recording                            | `/api/v1/metrics`, probe route                | probes -> recordNetworkTelemetry -> metrics                          | PARTIAL      | Network metrics are updated after probes; full tracing pipeline not present.                                                                |
| Electron UI        | Electron main/preload/renderer                                                | IPC contract                                        | `registerIpc()` in main process                            | renderer pages                                | renderer -> preload platform -> IPC -> demo data                     | PARTIAL      | Electron starts after OS libs/Xvfb and smoke env; UI data is DEMO, not live backend.                                                        |
| Cross-system E2E   | Existing backend and desktop paths                                            | HTTP + Electron IPC                                 | separate processes                                         | manual runtime checks                         | backend live network path and Electron demo IPC path remain separate | FAIL         | No real Backend -> Core -> DNS/Routing/Tunnel/AI -> Event Bus -> Electron -> UI chain exists.                                               |
