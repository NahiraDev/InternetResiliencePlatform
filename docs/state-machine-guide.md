# State Machine Guide

Engines expose strongly typed state machines using explicit transitions. VPN, DNS, proxy, routing, workflow, plugin, and node engines can share the kernel `StateMachine` primitive.
