# Cross-System E2E

The executable gate is `pnpm phase23:e2e`. It proves one authoritative runtime decision can be serialized through the runtime core, API DTO envelope, CLI-compatible JSON, and Electron-compatible JSON without duplicating state. The gate uses safe deterministic providers/adapters, not demo fixtures and not host mutation.
