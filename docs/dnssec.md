# DNSSEC

DNSSEC support tracks provider capability, validation status, trust-anchor configuration, and validation failures. The engine can require DNSSEC validation when configured and exposes DNSSEC status through `/dns/status`, `irp dns status`, and provider ranking metadata.
