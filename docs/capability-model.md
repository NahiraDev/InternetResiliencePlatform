# Capability Model

Every kernel operation requires a capability. Built-in capabilities include DNS resolution and modification, VPN connection management, proxy forwarding, network capture/routing/inspection, storage, crypto, telemetry, plugin installation/loading, scheduler execution, and workflow runs.
