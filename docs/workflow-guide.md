# Workflow Guide

Workflows define triggers and ordered steps. Steps include conditions, required capabilities, actions, input, retry, timeout-compatible metadata, and simulation mode. Simulation returns predicted steps without invoking handlers.
