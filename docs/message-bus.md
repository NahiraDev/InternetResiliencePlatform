# Message Bus

The internal bus supports command, query, event, broadcast, and pipeline messages. Messages include priority, retry count, persistence flag, cancellation through the kernel abort signal, and optional ordering keys for distributed compatibility.
