# DNS Rule Engine

Rules support domain, wildcard, suffix, regular expression, TLD, IP range, CIDR, protocol, and network-interface match dimensions. Actions include force provider, block, bypass, prefer provider, fallback provider, and custom routing profile. Higher-priority rules win, making conflict resolution deterministic.
