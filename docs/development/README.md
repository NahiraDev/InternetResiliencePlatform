# Development Guide

## Prerequisites

Phase 0 requires a POSIX shell and Git. Future phases may add language-specific tooling.

## Local commands

```bash
./scripts/bootstrap.sh
./scripts/lint.sh
./scripts/test.sh
```

The scripts are intentionally lightweight placeholders until application code is introduced.
