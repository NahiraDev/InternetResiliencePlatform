{
  "name": "@irp/linux-client",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "pretypecheck": "pnpm -r --filter @irp/resilience-runtime... run build",
    "pretest": "pnpm -r --filter @irp/resilience-runtime... run build",
    "prebuild": "pnpm -r --filter @irp/resilience-runtime... run build"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "@irp/resilience-runtime": "workspace:*"
  }
}
