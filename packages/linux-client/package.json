{
  "name": "@irp/linux-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run",
    "@irp/resilience-runtime": "workspace:*",
    "@irp/telemetry": "workspace:*",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  }
}
