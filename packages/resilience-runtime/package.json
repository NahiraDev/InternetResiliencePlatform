{
  "name": "@irp/resilience-runtime",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/resilience-runtime/src/index.js",
  "types": "dist/resilience-runtime/src/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "coverage": "vitest run --config vitest.config.ts --coverage"
  },
  "dependencies": {
    "@irp/events": "workspace:*",
    "@irp/telemetry": "workspace:*",
    "@irp/failover": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "@vitest/coverage-v8": "^4.1.10"
  }
}
