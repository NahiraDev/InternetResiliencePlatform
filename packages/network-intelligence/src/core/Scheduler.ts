export interface SchedulerOptions { intervalMs: number; runImmediately: boolean; }
export class Scheduler { private timer: NodeJS.Timeout | undefined; private running = false; constructor(private readonly task: (signal: AbortSignal) => Promise<void>, private readonly options: SchedulerOptions) {} start(signal: AbortSignal): void { if (this.timer) return; const run=(): void=>{ if(this.running) return; this.running=true; void this.task(signal).finally(()=>{ this.running=false; }); }; if(this.options.runImmediately) run(); this.timer=setInterval(run, this.options.intervalMs); } stop(): void { if(this.timer) clearInterval(this.timer); this.timer=undefined; } isRunning(): boolean { return this.timer !== undefined; } }
