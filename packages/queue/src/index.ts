export interface QueueMessage<T = unknown> { id: string; name: string; payload: T; attempts: number; enqueuedAt: Date; }
export type QueueHandler<T = unknown> = (message: QueueMessage<T>) => Promise<void> | void;
export interface Queue { enqueue<T>(name: string, payload: T): Promise<QueueMessage<T>>; process<T>(name: string, handler: QueueHandler<T>): void; size(name?: string): number; }
export class MemoryQueue implements Queue { private readonly messages: QueueMessage[] = []; private readonly handlers = new Map<string, QueueHandler>(); async enqueue<T>(name: string, payload: T): Promise<QueueMessage<T>> { const message: QueueMessage<T> = { id: crypto.randomUUID(), name, payload, attempts: 0, enqueuedAt: new Date() }; this.messages.push(message); const handler = this.handlers.get(name) as QueueHandler<T> | undefined; if (handler) { message.attempts += 1; await handler(message); this.messages.splice(this.messages.indexOf(message), 1); } return message; } process<T>(name: string, handler: QueueHandler<T>): void { this.handlers.set(name, handler as QueueHandler); } size(name?: string): number { return name ? this.messages.filter((message) => message.name === name).length : this.messages.length; } }
