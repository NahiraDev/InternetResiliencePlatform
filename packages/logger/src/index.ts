import pino, { type Logger as PinoLogger } from 'pino';
export type LogLevel = 'debug' | 'info' | 'warn' | 'error';
export interface LogRecord { timestamp: string; level: LogLevel; message: string; context?: Record<string, unknown>; }
export interface Transport { write(record: LogRecord): void; }
export class Logger { private readonly rank: Record<LogLevel, number> = { debug: 10, info: 20, warn: 30, error: 40 }; constructor(private readonly transports: Transport[], private readonly minLevel: LogLevel = 'info') {} log(level: LogLevel, message: string, context?: Record<string, unknown>): void { if (this.rank[level] < this.rank[this.minLevel]) return; const record: LogRecord = { timestamp: new Date().toISOString(), level, message, ...(context ? { context } : {}) }; this.transports.forEach((transport) => transport.write(record)); } debug(message: string, context?: Record<string, unknown>): void { this.log('debug', message, context); } info(message: string, context?: Record<string, unknown>): void { this.log('info', message, context); } warn(message: string, context?: Record<string, unknown>): void { this.log('warn', message, context); } error(message: string, context?: Record<string, unknown>): void { this.log('error', message, context); } child(bindings: Record<string, unknown>): Logger { return new Logger(this.transports.map((transport) => ({ write: (record) => transport.write({ ...record, context: { ...bindings, ...record.context } }) })), this.minLevel); } }
export interface LoggerOptions { level?: LogLevel; pretty?: boolean; service?: string; }
class PinoTransport implements Transport { constructor(private readonly instance: PinoLogger) {} write(record: LogRecord): void { this.instance[record.level](record.context ?? {}, record.message); } }
export const createLogger = (options: LoggerOptions | LogLevel = {}): Logger => { const normalized = typeof options === 'string' ? { level: options } : options; const instance = pino({ level: normalized.level ?? 'info', base: { service: normalized.service ?? 'internet-resilience-platform' }, ...(normalized.pretty ? { transport: { target: 'pino-pretty', options: { colorize: true, singleLine: true } } } : {}) }); return new Logger([new PinoTransport(instance)], normalized.level ?? 'info'); };
