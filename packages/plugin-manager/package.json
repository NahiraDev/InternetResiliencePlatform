{
  "name": "@irp/plugin-manager",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "@irp/plugin-runtime": "workspace:*",
    "@irp/plugin-loader": "workspace:*",
    "@irp/plugin-registry": "workspace:*",
    "@irp/plugin-sdk": "workspace:*",
    "@irp/plugin-samples": "workspace:*"
  }
}
