{
  "name": "@irp/daemon",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@irp/config": "workspace:*",
    "@irp/connectivity": "workspace:*",
    "@irp/core": "workspace:*",
    "@irp/dns": "workspace:*",
    "@irp/gateway-registry": "workspace:*",
    "@irp/logger": "workspace:*",
    "@irp/plugin-manager": "workspace:*",
    "@irp/resilience-runtime": "workspace:*",
    "@irp/routing": "workspace:*",
    "@irp/tunnel": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "typescript": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
